{
  "name": "sandbox-adapter",
  "private": true,
  "version": "0.0.1",
  "description": "Thin Cloudflare Worker wrapping @cloudflare/sandbox. Deliberately dumb — see /docs/ADR-001-language-choice.md. No pricing, payment, or business logic belongs in this package.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --config wrangler.deploy.jsonc"
  },
  "dependencies": {
    "@cloudflare/sandbox": "0.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "wrangler": "latest",
    "typescript": "latest"
  }
}
